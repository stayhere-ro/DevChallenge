# Submission

## What I did
I choose to start with the level 1 tasks because:
- I have little experience with php
- A post endpoint is great to go throw all of the flows, from api call right to the end, a db insert
- Wanted to make the feature test for it, so it can be easier tested, not just manually with a curl call, or using postman
- Wanted to make the Bookings seeder, but I didn't had enough time for it...

## Tasks done
- managed to finish tasks:
    - 1
    - 2
    - 4
    - 5

## Testing
- setup using setup.bat and change the db in env
- php artisan test tests/Feature/ApiBookingTest.php - this command can be used to run the post endpoints feauter tests
- php artisan bookings:list test@example.com - list bookings related to the test email (entries are generated by the previous tests↑)
- for testing the  GET `/api/bookings?email=...` endpoint 
    - use: curl -X GET "http://127.0.0.1:8000/api/bookings?email=client1@example.com"